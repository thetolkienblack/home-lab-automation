#!/bin/bash
# Generated by Ansible - elk_stack role
# CheckMK agent plugin for Logstash monitoring

echo "<<<logstash_stats>>>"
curl -s http://localhost:{{ logstash_http_port }}/_node/stats 2>/dev/null

echo "<<<logstash_pipelines>>>"
curl -s http://localhost:{{ logstash_http_port }}/_node/stats/pipelines 2>/dev/null
