#!/bin/bash
# Generated by Ansible - elk_stack role
# CheckMK agent plugin for Elasticsearch monitoring

echo "<<<elasticsearch_cluster>>>"
curl -s http://localhost:{{ elasticsearch_http_port }}/_cluster/health 2>/dev/null

echo "<<<elasticsearch_nodes>>>"
curl -s http://localhost:{{ elasticsearch_http_port }}/_nodes/stats 2>/dev/null

echo "<<<elasticsearch_indices>>>"
curl -s http://localhost:{{ elasticsearch_http_port }}/_cat/indices?format=json 2>/dev/null
